using System;
using System.Collections.Generic;
using System.Text;

namespace fitnessTracker.core
{
    public static class DiscretePlanGenerator
    {
        public static DiscreteExercisePlan Generate(string name, FitnessProfile profile, int sets, int max)
        {
            var plan = new DiscreteExercisePlan();
            
            return new DiscreteExercisePlan();
        }
    }
}
